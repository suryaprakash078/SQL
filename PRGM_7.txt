SET SERVEROUTPUT ON;

DECLARE
    a NUMBER := 1;
BEGIN
    DBMS_OUTPUT.PUT_LINE('Program started');

    LOOP
        DBMS_OUTPUT.PUT_LINE(a);
        a := a + 1;
        EXIT WHEN a > 5;
    END LOOP;

    DBMS_OUTPUT.PUT_LINE('Program completed');
END;
/